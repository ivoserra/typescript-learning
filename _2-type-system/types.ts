const now = new Date()
now.getFullYear();

const track = {duration:120}

class Planet {}

const earth = new Planet


// Types annotation & Type Infrence 

// type annotation tells js what type the value has.

// type inference is the system that typescript uses to guess what the type is